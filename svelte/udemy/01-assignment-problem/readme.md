1. add an input field that allows users to enter a course goal
2. output the user input in a H1 tag
3. color the output in red if it contains at least one exclamation mark
4. put the h1 tag + output in a separate component to which you pass the user input